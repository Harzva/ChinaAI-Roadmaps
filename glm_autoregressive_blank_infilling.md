# GLM: General Language Model Pretraining with Autoregressive Blank Infilling

《GLM: General Language Model Pretraining with Autoregressive Blank Infilling》是智谱 AI 与清华大学提出的一篇重要论文，首次将填空任务与自回归语言模型统一起来。传统的掩码语言模型（如 BERT）在生成长文本时效果欠佳，而单向自回归模型又缺乏对未来信息的理解。GLM 使用**自回归填空**策略：在预训练阶段随机遮盖文本的多个片段，并让模型按遮盖顺序预测各片段的内容，同时条件于未遮挡的上下文和已经预测的片段。这种方法兼顾了双向语境理解和连续文本生成能力。

## 主要贡献

* **统一生成与理解**：GLM 提供了一种统一框架，通过自回归填空既能进行句子级理解，也能自然生成完整文本。
* **灵活的掩码策略**：遮盖长度和位置均可调节，使模型学习到跨句子乃至跨段落的全局依赖。
* **兼容下游任务**：GLM 在多项语言理解和生成基准上表现良好，为后来的大模型如 GLM‑130B、ChatGLM 奠定基础。

## 影响

GLM 论文为后续的 GLM 系列模型提供了理论出发点，使模型可以同时具备生成和理解能力。这种填空式预训练思想也启发了后续的诸多研究，是中文大模型领域的重要里程碑。

## 问题与回答

1. **问：这篇论文解决了什么问题？**
   **答：** 论文提出了一种新的预训练策略——自回归填空（blank infilling），旨在弥合自编码（BERT 类）和自回归（GPT 类）方法的差距，既保留表示学习能力，又具备文本生成能力。

2. **问：GLM 与 BERT 的主要区别是什么？**
   **答：** BERT 使用掩码语言模型专注于编码文本，而 GLM 在训练时在文本中插入随机留白，然后要求模型顺序填充这些留白，从而兼顾理解和生成。

3. **问：自回归填空机制如何工作？**
   **答：** 在输入序列中随机采样若干片段替换为空白标记，模型按照空白出现顺序逐个填充，每次填空时利用前面已恢复的文本作为上下文，使模型学会条件生成。

4. **问：预训练的数据集有哪些特点？**
   **答：** 论文使用大量中英文语料进行双语预训练，包括维基百科、新闻和图书等开放文本，确保模型在多任务和跨语言环境下的泛化能力。

5. **问：模型在哪些任务上得到提升？**
   **答：** GLM 在自然语言理解（如问答、文本分类）和生成任务（如摘要、翻译）上都取得了比传统自编码模型更优的结果。

6. **问：它如何处理长上下文信息？**
   **答：** 自回归填空采用块结构打包输入，训练时对长序列随机留空，使模型学会在更长距离的上下文中预测，从而具备长文本建模能力。

7. **问：GLM 的训练目标如何结合两种预训练方式？**
   **答：** 模型同时优化自编码和自回归的目标，通过填空任务训练生成能力，通过剩余部分的条件概率学习语言理解能力，形成统一框架。

8. **问：与 GPT 或其他模型相比效果如何？**
   **答：** 实验表明，在相同参数规模下，GLM 在理解任务上优于 GPT 且接近 BERT，在生成任务上优于 BERT，同时保持推理的流畅性和准确性。

9. **问：训练过程中的重要超参数有哪些？**
   **答：** 包括留空比例、片段长度分布、学习率和批次大小等。论文推荐留空比例约为 15%，片段长度多样，以提高模型的泛化能力。

10. **问：后续工作中如何利用 GLM？**
    **答：** GLM 成为 ChatGLM 系列模型的基础，为后续多模态扩展、工具调用和强化学习微调奠定了模型架构和训练思想基础。
