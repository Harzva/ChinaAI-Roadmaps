# QA Notes

- `node --check src/data.js` passed.
- `node --check src/app.js` passed.
- No CDN, Mapbox token, backend, or build step is required.
- All project paths are relative, so the site can run from a GitHub Pages project path.
